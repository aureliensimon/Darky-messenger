{
  "scripts": {
    "watch": "sass --watch --no-source-map style.scss src/style.css",
    "transpile": "sass --no-source-map style.scss src/style.css"
  },
  "devDependencies": {
    "sass": "^1.22.12"
  },
  "dependencies": {
    "scss": "^0.2.4"
  }
}
